#logging.level.org.testcontainers=DEBUG
#logging.level.io.r2dbc=DEBUG
spring.r2dbc.url=r2dbc:postgres://${embedded.postgresql.host}:${embedded.postgresql.port}/${embedded.postgresql.schema}
spring.r2dbc.username=${embedded.postgresql.user}
spring.r2dbc.password=${embedded.postgresql.password}